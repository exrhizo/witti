<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:screenOrientation="portrait"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:paddingBottom="@dimen/activity_vertical_margin"
    android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingTop="@dimen/activity_vertical_margin"
    tools:context=".HomeActivity" >

    <TextView
        android:id="@+id/titleText"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_centerHorizontal="true"
        android:textSize="70sp"
        android:textStyle="bold"
        android:textColor="#01A9DB"
        android:text="@string/app_name" />
    	 
    <TextView 
        android:layout_width="wrap_content"
        android:layout_height="wrap_content" 
        android:layout_below="@id/titleText"
        android:text="@string/authors"
        android:id="@+id/authorsTextView"
        android:textStyle="bold"
        android:layout_alignLeft="@+id/createdForTextView" />
    
    <TextView 
        android:layout_width="wrap_content"
        android:layout_height="wrap_content" 
        android:layout_below="@id/authorsTextView"
        android:text="@string/authorNames"
        android:id="@+id/authorNamesTextView"
        android:layout_alignLeft="@id/createdForTextView" /> 	
    
    <TextView 
        android:layout_width="wrap_content"
        android:layout_height="wrap_content" 
        android:layout_below="@id/authorNamesTextView"
        android:text="@string/createdFor"
        android:id="@id/createdForTextView"
        android:textStyle="bold"
        android:layout_centerHorizontal="true" />
    
    <TextView 
        android:layout_width="wrap_content"
        android:layout_height="wrap_content" 
        android:layout_below="@id/createdForTextView"
        android:text="@string/classDetails"
        android:id="@+id/classDetailsTextView"
        android:layout_alignLeft="@id/createdForTextView"  />
    

</RelativeLayout>